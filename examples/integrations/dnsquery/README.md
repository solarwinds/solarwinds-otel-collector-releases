# DNS Query Integration Example

This directory contains an example configuration for integrating DNS Query. This configuration is compatible with SolarWinds Observability SaaS.

- `config.yaml`: Example configuration file for DNS Query integration.
