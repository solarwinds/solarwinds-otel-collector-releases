# Host Example

This directory contains an example configuration for host monitoring. This configuration is compatible with SolarWinds Observability SaaS.

> [!NOTE]
> Please note that Host monitoring in Docker containers requires setting the `bios_uuid` field of the `uuid` processor manually to ensure proper integration with SolarWinds Observability SaaS.

- `config.yaml`: Example configuration file for Host monitoring.
