# Postgresql Integration Example

This directory contains an example configuration for integrating Postgresql. 
This configuration is compatible with SolarWinds Observability SaaS.

- `config.yaml`: Example configuration file for Postgresql integration.

## Prerequisites

The monitoring user must be granted SELECT on pg_stat_database.
