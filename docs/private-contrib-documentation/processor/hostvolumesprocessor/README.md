# hostvolumes processor

<!-- distribution links hack -->
[verified]: https://github.com/solarwinds/solarwinds-otel-collector-releases#verified
[playground]: https://github.com/solarwinds/solarwinds-otel-collector-releases#playground
[k8s]: https://github.com/solarwinds/solarwinds-otel-collector-releases#k8s

<!-- status autogenerated section -->
| Status        |           |
| ------------- |-----------|
| Stability     | [development]: metrics   |
| Distributions | [playground], [verified] |

[development]: https://github.com/open-telemetry/opentelemetry-collector/blob/main/docs/component-stability.md#development
[playground]: 
[verified]: 
<!-- end autogenerated section -->

`hostvolumes` processor enhances `system.filesystem.utilization` and `system.filesystem.utilization` metrics with datapoints related to folder mounted volumes.

The Processor affects Windows machines only, it has no effect when used on hosts running on other platforms.

## General processor configuration

There is no specific configuration defined for the processor. It can be simply declared among other processors:

```yaml
processors:
  hostvolumes:
```
