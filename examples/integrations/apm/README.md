# APM Integration Example

This directory contains an example configuration for integrating APM.
This configuration is compatible with SolarWinds Observability SaaS.

- `config.yaml`: Example configuration file for APM integration.
